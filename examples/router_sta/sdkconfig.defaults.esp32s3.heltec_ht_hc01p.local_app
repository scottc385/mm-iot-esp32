#
# Optional diagnostic profile. The in-process BACnet local app is intentionally
# disabled because the virtual Device object is not fully compliant and can
# confuse client discovery/property reads.
#
# CONFIG_ROUTER_STA_LOCAL_APP_ENABLE is not set

#
# The local-app overlay pulls in more router/BACnet code and previously crashed
# during the Morse connect path before local-app traffic executed. Keep these
# stack values higher than the normal profile while isolating memory pressure.
#
CONFIG_ESP_MAIN_TASK_STACK_SIZE=8192
CONFIG_FREERTOS_ISR_STACKSIZE=2048
